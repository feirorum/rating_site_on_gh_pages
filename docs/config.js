// Update these values to point at the repository that stores your issues.
// The static site will fetch public data from GitHub's REST API.
window.RATINGS_HUB_CONFIG = {
  owner: 'your-org',
  repo: 'ratings-hub',
  label: 'type:item'
};
